# Tembo Agent Studio — v0.1 skeleton.
# Single-node target per context/0.1/README.md.
# Usage:
#   cp .env.example .env  # fill in secrets
#   docker compose up --build

services:
  postgres:
    image: postgres:18-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-tas}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-tas}
      POSTGRES_DB: ${POSTGRES_DB:-tas}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      # postgres:18+ stores data in a version subdir under
      # /var/lib/postgresql (not /var/lib/postgresql/data) — mount the
      # parent so the image manages the layout.
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-tas} -d ${POSTGRES_DB:-tas}"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-tas}:${POSTGRES_PASSWORD:-tas}@postgres:5432/${POSTGRES_DB:-tas}
      TAS_ENCRYPTION_KEY: ${TAS_ENCRYPTION_KEY:?TAS_ENCRYPTION_KEY must be set in .env}
      INTERNAL_API_TOKEN: ${INTERNAL_API_TOKEN:?INTERNAL_API_TOKEN must be set in .env}
      API_MAX_CONCURRENT_RUNS: ${API_MAX_CONCURRENT_RUNS:-4}
      API_RESERVED_CHILD_RUNS: ${API_RESERVED_CHILD_RUNS:-1}
      RUST_LOG: ${RUST_LOG:-info,tas_api=debug}
    ports:
      - "${API_PORT:-8080}:8080"
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://localhost:8080/health > /dev/null || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 30s

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-tas}:${POSTGRES_PASSWORD:-tas}@postgres:5432/${POSTGRES_DB:-tas}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:?BETTER_AUTH_SECRET must be set in .env}
      TAS_ENCRYPTION_KEY: ${TAS_ENCRYPTION_KEY:?TAS_ENCRYPTION_KEY must be set in .env}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL:-http://localhost:3000}
      API_INTERNAL_URL: http://api:8080
      INTERNAL_API_TOKEN: ${INTERNAL_API_TOKEN:?INTERNAL_API_TOKEN must be set in .env}
      NEXT_PUBLIC_BETTER_AUTH_URL: ${BETTER_AUTH_URL:-http://localhost:3000}
      TAS_INSTANCE_NAME: ${TAS_INSTANCE_NAME:-Tembo Agent Studio}
      INSTANCE_ADMIN_EMAILS: ${INSTANCE_ADMIN_EMAILS:-}
      # Dev: when a workspace has no connected GitHub repo, load agents from the
      # mounted samples dir (repo-shaped: contains agents/<framework>/…). Unset
      # this (or connect a repo) to use GitHub instead.
      TAS_LOCAL_AGENTS_DIR: ${TAS_LOCAL_AGENTS_DIR:-/samples}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET:-}
      MICROSOFT_CLIENT_ID: ${MICROSOFT_CLIENT_ID:-}
      MICROSOFT_CLIENT_SECRET: ${MICROSOFT_CLIENT_SECRET:-}
      MICROSOFT_TENANT_ID: ${MICROSOFT_TENANT_ID:-}
      OIDC_DISCOVERY_URL: ${OIDC_DISCOVERY_URL:-}
      OIDC_CLIENT_ID: ${OIDC_CLIENT_ID:-}
      OIDC_CLIENT_SECRET: ${OIDC_CLIENT_SECRET:-}
      OIDC_PROVIDER_NAME: ${OIDC_PROVIDER_NAME:-}
      OIDC_SCOPES: ${OIDC_SCOPES:-}
      SLACK_CLIENT_ID: ${SLACK_CLIENT_ID:-}
      SLACK_CLIENT_SECRET: ${SLACK_CLIENT_SECRET:-}
      GOOGLE_CONNECTIONS_CLIENT_ID: ${GOOGLE_CONNECTIONS_CLIENT_ID:-}
      GOOGLE_CONNECTIONS_CLIENT_SECRET: ${GOOGLE_CONNECTIONS_CLIENT_SECRET:-}
    ports:
      - "${WEB_PORT:-3000}:3000"
    volumes:
      # Sample agents for dev: mounted repo-shaped at /samples/agents so the
      # local agent source (TAS_LOCAL_AGENTS_DIR=/samples) finds
      # agents/<framework>/…. Read-only; harmless when a real repo is connected.
      - ./agents:/samples/agents:ro

volumes:
  postgres_data:
